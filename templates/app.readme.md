# {{APP_NAME}} API

## Overview

The {{APP_NAME}} API provides [brief description of the API's purpose and functionality].

## Features

This API includes the following features:

<!-- Features will be automatically populated as they are added -->

## Getting Started

### Prerequisites

- Node.js 18+ 
- npm 9+

### Development

1. Start the development server:
   ```bash
   npm run dev:api
   ```

2. The API will be available at `http://localhost:3000/api/{{APP_NAME}}`

### Testing

Run unit tests:
```bash
npm run test unit:run {{APP_NAME}}
```

Run API tests:
```bash
npm run test api:run {{APP_NAME}}
```

Run all tests:
```bash
npm run test all {{APP_NAME}}
```

## API Documentation

### Base URL
- Development: `http://localhost:3000/api/{{APP_NAME}}`
- Production: `https://your-domain.com/api/{{APP_NAME}}`

### Endpoints

<!-- API endpoints will be documented here -->

## Configuration

The API behavior can be configured through `{{APP_NAME}}.api.config.json`.

## Compliance

This API follows Voila framework compliance standards. Run compliance checks with:

```bash
npm run compliance {{APP_NAME}}
```

## Support

For questions or issues, contact the {{APP_NAME}} Team.